# Codentra Android App

Android WebView app for Codentra.

## API / Base URL
The app loads from:
- `https://www.codentra.store`

Defined in `app/build.gradle.kts` as:
- `BuildConfig.API_BASE_URL`

## Run
1. Open folder `android app` in Android Studio.
2. Let Gradle sync.
3. Run on emulator/device.
